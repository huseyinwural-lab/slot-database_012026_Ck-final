# Security headers entrypoint (P4.3)
#
# Rollback in <5 minutes design:
# - This file is the ONLY file you switch in deployments.
# - Choose exactly one include below.
#
# Example (report-only stage):
#   include /etc/nginx/snippets/security_headers_report_only.conf;
#
# Example (enforce stage):
#   include /etc/nginx/snippets/security_headers_enforce.conf;
#
# Example (break-glass OFF):
#   # (comment out all includes)

# include /etc/nginx/snippets/security_headers_report_only.conf;
# include /etc/nginx/snippets/security_headers_enforce.conf;

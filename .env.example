# Root env template (docs-smoke expects this file)
# Backend: see backend/.env.example
# Frontend: see frontend/.env.example

ENV=
JWT_SECRET=
DATABASE_URL=
SYNC_DATABASE_URL=
CORS_ORIGINS=

# Frontend
REACT_APP_BACKEND_URL=http://localhost:8001

# Mongo (template compatibility)
MONGO_URL=
DB_NAME=

# Redis
REDIS_URL=

# Email (Resend)
RESEND_API_KEY=
RESEND_FROM=
RESEND_TEST_TO=


# Database
POSTGRES_PASSWORD=
DB_POOL_SIZE=
DB_MAX_OVERFLOW=

# Velocity Limits
REGISTER_VELOCITY_LIMIT=100

# One-shot owner bootstrap (prod/staging)
BOOTSTRAP_ENABLED=false
BOOTSTRAP_OWNER_EMAIL=
BOOTSTRAP_OWNER_PASSWORD=
BOOTSTRAP_OWNER_TENANT_ID=default_casino

# Seeding (dev/local only)
SEED_ON_STARTUP=false

# Backend extras (from backend/.env.example)
DISABLE_RATE_LIMITS=
KYC_MOCK_ENABLED=
KYC_UNVERIFIED_DAILY_DEPOSIT_CAP=
MAX_TX_VELOCITY_COUNT=
MAX_TX_VELOCITY_WINDOW_MINUTES=
ALLOW_TEST_PAYMENT_METHODS=
PAYMENT_HOLD_MINUTES=
